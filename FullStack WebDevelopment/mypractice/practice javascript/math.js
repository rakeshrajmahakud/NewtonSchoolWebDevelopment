// note:
// Math.round() - rounds to the nearest integer (if the fraction is 0.5 or greater - rounds up)
// Math.floor() - rounds down
// Math.round(3.14159)  // 3
// Math.round(3.5)      // 4
// Math.floor(3.8)      // 3 returns the value of x rounded down to its nearest integer
// Math.ceil(3.2)       // 4  returns the value of x rounded up to its nearest integer 

// Math.PI    // returns value of pi
// Math.pow(x,y) // it takes two number andcalculste the power 
// Math.sqrt(x); // returns the squreroot
// Math.abs(-x); // it returns the absolute value (positive value of negitive number )
//Math.min(x,y,z,...) it returns minimum among them
//Math.max(x,y,z,...) it returns maximum among them
// Math.trunc()// which removes decimal part and returns integer only like parseInt()

//Math.random    // returns random decimal value in between 0 and 1

