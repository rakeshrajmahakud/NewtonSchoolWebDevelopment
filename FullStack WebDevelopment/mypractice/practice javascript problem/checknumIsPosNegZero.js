// we can use Math.sign to check the value is either positive or nrgrtive

var x = -5;
var res = Math.sign(x);
if (res === 1) {
    console.log(`the numer is positive`);
}else{
    console.log(`the numer is negetive`);
}
 